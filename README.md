# IFFA-TSCI-CNN
This project implements the **IFFA-TSCI-CNN methodology** for intelligent ARIMA(p,d,q) parameter selection. Instead of manually testing hundreds of ARIMA configurations, the CNN learns to predict the optimal parameters by recognizing patterns in time series when encoded as images.
